import fs from'node:fs';import http from'node:http';import path from'node:path';import{chromium,firefox}from'playwright';
const root=process.cwd(),server=http.createServer((q,r)=>{let p=q.url==='/'?'index.html':q.url.slice(1);p=path.normalize(p).replace(/^\.\.(\/|\\)/,'');let f=path.join(root,p);if(!f.startsWith(root)||!fs.existsSync(f)){r.statusCode=404;return r.end('no')}r.setHeader('content-type',p.endsWith('.js')?'text/javascript':p.endsWith('.css')?'text/css':'text/html');r.end(fs.readFileSync(f))});await new Promise(ok=>server.listen(4174,'127.0.0.1',ok));
try{for(const[n,b]of[['chromium',chromium],['firefox',firefox]]){const browser=await b.launch({headless:true}),page=await browser.newPage({viewport:{width:1280,height:720}}),errors=[];page.on('pageerror',e=>errors.push(String(e)));await page.goto('http://127.0.0.1:4174/');await page.waitForFunction(()=>typeof state!='undefined'&&state==='title'&&typeof zone!='undefined');let z=await page.evaluate(()=>zone);await page.locator('canvas').dblclick({position:{x:640,y:585},delay:20});let z2=await page.evaluate(()=>zone);if(z2!==(z+1)%3)throw Error(n+' title double-click changed world incorrectly: '+z+' -> '+z2);await page.keyboard.press('Enter');await page.waitForFunction(()=>state==='play');let anti=await page.evaluate(()=>{let u=makeUni(5,95,160),o={x:100,y:100,w:120,h:120,hp:100,max:100,val:0,t:'b'};u.live=1;u.order=0;u.ai=0;u.vx=120;u.vy=0;unis.push(u);collide(u,o,.016);return{ai:u.ai,tx:u.tx,ty:u.ty,x:u.x,y:u.y}});if(!(anti.ai>0&&Number.isFinite(anti.tx)&&Number.isFinite(anti.ty)))throw Error(n+' anti-stuck retarget failed '+JSON.stringify(anti));for(let i=0;i<60;i++)await page.mouse.click(20,20);if(page.context().pages().length!==1||!page.url().startsWith('http://127.0.0.1:4174/'))throw Error(n+' rapid clicks escaped game page');if(errors.length)throw Error(n+' page errors: '+errors.join(' | '));await browser.close();console.log(n+' showcase browser smoke: PASS')}}finally{server.close()}
